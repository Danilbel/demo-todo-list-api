server:
  port: 9120

spring:
  datasource:
    url: jdbc:postgresql://postgresql:5432/demo_todo_list
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true